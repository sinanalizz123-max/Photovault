<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Main Content -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="match_parent"
        android:orientation="vertical" android:background="?attr/colorSurface">

        <!-- App Bar -->
        <LinearLayout android:id="@+id/app_bar"
            android:layout_width="match_parent" android:layout_height="60dp"
            android:orientation="horizontal" android:gravity="center_vertical"
            android:paddingStart="12dp" android:paddingEnd="12dp"
            android:background="?attr/colorSurface" android:elevation="4dp">
            <ImageButton android:id="@+id/btn_drawer"
                android:layout_width="40dp" android:layout_height="40dp"
                android:src="@drawable/ic_menu" android:background="@drawable/bg_pill"
                android:padding="8dp" android:tint="?attr/colorOnSurface" android:contentDescription="Menu"/>
            <TextView android:id="@+id/tv_title"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="Photos"
                android:textSize="20sp" android:textStyle="bold"
                android:textColor="?attr/colorOnSurface" android:paddingStart="12dp"/>
            <ImageButton android:id="@+id/btn_search"
                android:layout_width="40dp" android:layout_height="40dp"
                android:src="@drawable/ic_search" android:background="@drawable/bg_pill"
                android:padding="8dp" android:tint="?attr/colorOnSurface" android:contentDescription="Search"/>
            <ImageButton android:id="@+id/btn_upload"
                android:layout_width="40dp" android:layout_height="40dp"
                android:src="@drawable/ic_upload" android:background="@drawable/bg_pill"
                android:padding="8dp" android:tint="@color/accent_blue"
                android:layout_marginStart="6dp" android:contentDescription="Upload Queue"/>
        </LinearLayout>

        <!-- Fragment Container -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent" android:layout_height="0dp"
            android:layout_weight="1"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_graph"/>

        <!-- Bottom Navigation -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav"
            android:layout_width="match_parent" android:layout_height="64dp"
            android:background="?attr/colorSurface"
            app:menu="@menu/bottom_nav_menu"
            app:itemIconTint="@color/nav_icon_color"
            app:itemTextColor="@color/nav_text_color"
            app:labelVisibilityMode="labeled"
            style="@style/Widget.PhotoVault.BottomNavigationView"/>
    </LinearLayout>

    <!-- Left Drawer -->
    <include layout="@layout/drawer_left" android:id="@+id/left_drawer"
        android:layout_width="290dp" android:layout_height="match_parent"
        android:layout_gravity="start"/>

</androidx.drawerlayout.widget.DrawerLayout>
